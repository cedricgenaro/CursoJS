let width = 1920;
let height = 1080;

function totalPixels(w, h){
    return w * h;
}

console.log(`Um monitor de tamanho ${width} x ${height} tem a quantidade de pixels = `);
console.log(totalPixels(width, height));