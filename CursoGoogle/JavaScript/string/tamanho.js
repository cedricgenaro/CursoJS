let message = 'O dia hoje está ensolarado mas está bastante quente a previsão é que a noite esfrie.';
console.log(message);
/* Usando a propriedade length */
if (message.length > 80){
    console.log('A mensagem é muito longa para um cartão postal');
}else{
    console.log('A mensagem é perfeita para um cartão postal');
}
